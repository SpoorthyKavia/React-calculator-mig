# React-calculator-mig

This project includes a **Dark Mode toggle** that:
- toggles between light/dark themes,
- applies theme via `data-theme` on the `<html>` element,
- persists the user preference in `localStorage`.

## Run locally

```bash
npm install
npm run dev
```
