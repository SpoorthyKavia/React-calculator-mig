# React-calculator-mig

A simple single-page React calculator with basic arithmetic, history, and error handling.

## Scripts

- `npm install`
- `npm run start` (dev server on port 3000)
- `npm run build`
- `npm run preview` (serve the production build on port 3000)
